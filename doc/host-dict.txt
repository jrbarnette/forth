things to redefine for use in host-target search order
    HERE ALLOT ALIGN , C,
    COMPILE,
    ' FIND
    IMMEDIATE
    VARIABLE CONSTANT CREATE
    : ]
    STATE

optional:
    UNUSED
    >BODY

for future consideration:
    @ ! +! C@ C! 2@ 2!
    ALIGNED CELL+ CELLS CHAR+ CHARS
    COUNT
    FILL MOVE ERASE

things to define for use in target compilation state:
    ( \ .(
    [ ;
    LITERAL S" ['] [CHAR]
    ."
    IF ELSE THEN
    BEGIN WHILE REPEAT UNTIL AGAIN
    DO I J LEAVE UNLOOP LOOP +LOOP
    DOES>
    POSTPONE

optional:
    ?DO
    ABORT"
    C"
    CASE
    ENDCASE
    ENDOF
    OF
    RECURSE
    [COMPILE]
